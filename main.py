import os, json, base64, logging, asyncio
from datetime import datetime, timedelta
import pytz
from flask import Flask, request
from telegram import Update, ReplyKeyboardMarkup, KeyboardButton
from telegram.ext import Application, CommandHandler, MessageHandler, ContextTypes, filters
import gspread
from google.oauth2.service_account import Credentials

# --- НАСТРОЙКИ ---
logging.basicConfig(level=logging.INFO)
log = logging.getLogger(__name__)

TELEGRAM_TOKEN = os.getenv("TELEGRAM_TOKEN")
PUBLIC_URL = os.getenv("PUBLIC_URL", "").rstrip('/')
GSHEET_ID = os.getenv("GSHEET_ID")
GOOGLE_SA_JSON_B64 = os.getenv("GOOGLE_SA_JSON_B64")

# --- БАЗА ДАННЫХ ИЗ CSV ---
DESC_LG = {
    "1": {"n": "Начало нового цикла", "d": "Время выбора направления на 9 лет. Приходит самый мощный энергетический поток.", "r": "Открывай свое дело, развивай лидерство, учись брать ответственность.", "m": "Жжение в области сердца, депрессия, чувство пустоты."},
    "2": {"n": "Год отношений и дипломатии", "d": "Период перемен в отношениях. Не принимай кардинальных решений.", "r": "Учись строить новые связи, мягко отпускай старое, сохраняй гибкость.", "m": "Болезненные разрывы, депрессия."},
    "3": {"n": "Год анализа и успеха", "d": "Пробуждается аналитическое мышление. Время успеха через расчет.", "r": "Действуй через анализ, планируй шаги на год вперед, веди учет.", "m": "Лень, азарт, корысть, поиск легкой наживы."},
    "4": {"n": "Год мистики и целей", "d": "Год постановки целей через неудовлетворенность. Время креатива.", "r": "Ставь цели, честно трудись, занимайся творчеством.", "m": "Желание мошенничать, апатия, риск судебных дел."},
    "5": {"n": "Год удачи и масштабирования", "d": "Год коммуникаций и фортуны. Время расширять границы.", "r": "Путешествуй, расширяй связи, строй бизнес через логику.", "m": "Борьба за справедливость, экстремизм, непостоянство."},
    "6": {"n": "Год успеха и комфорта", "d": "Время любви, инвестиций и пересмотра ценностей.", "r": "Занимайся домом, инвестируй, дари любовь и тепло.", "m": "Мстительность, лень, неразборчивые связи."},
    "7": {"n": "Год трансформации и кризиса", "d": "Отработка кармы. Время глубокого внутреннего роста.", "r": "Принимай ответственность, больше двигайся, не начинай новое.", "m": "Хаос, непонимание, отчаяние, рассеянность."},
    "8": {"n": "Год труда и обучения", "d": "Успех через дисциплину. Время наработки фундамента.", "r": "Учись, работай, покупай недвижимость. Не бери кредиты.", "m": "Усталость, перегрузка или полное бездействие."},
    "9": {"n": "Год служения и завершения", "d": "Завершение цикла. Подведение итогов.", "r": "Прости обиды, отдай старое, помогай людям, следи за здоровьем.", "m": "Эмоциональные всплески, разрушение без созидания."}
}

DESC_LM = {
    "1": "Хороший месяц для начала дел. Важна стратегия, планирование и дисциплина.",
    "2": "Месяц дипломатии. Активизируется энергия воспоминаний. Пей больше воды.",
    "3": "Месяц анализа. Сначала думай, потом делай. Удачно для обучения.",
    "4": "Месяц постановки целей. Креатив и честность помогут избежать иллюзий.",
    "5": "Месяц масштабирования. Отлично для бизнеса и новых связей.",
    "6": "Месяц любви и успеха. Интуиция на деньги. Время для инвестиций.",
    "7": "Месяц трансформации. Жизнь в дисциплине. Полезны йога и ходьба.",
    "8": "Месяц труда. Внимание к мелочам, контроль финансов.",
    "9": "Месяц завершения. Время служения и подведения итогов."
}

DESC_LD = {
    "1": "День начинаний. Реализуй стратегию, будь смелым.",
    "2": "День дипломатии. Слушай искренне, налаживай связи.",
    "3": "День анализа. Планируй, избегай азарта.",
    "4": "День мистики. Будь честен, ставь цели.",
    "5": "День масштабирования. Отлично для торговли.",
    "6": "День творчества. Создавай комфорт, дари заботу.",
    "7": "День трансформации. Дисциплина тела (ходьба).",
    "8": "День труда. Получай навыки, не бери кредиты.",
    "9": "День здоровья. Помогай людям, отдавай долги."
}

# --- ЛОГИКА ---
def reduce9(n):
    while n > 9: n = sum(map(int, str(n)))
    return n

def sync_user(update, birth=None, last_ym=None):
    try:
        user = update.effective_user
        uid = str(user.id)
        creds_json = json.loads(base64.b64decode(GOOGLE_SA_JSON_B64).decode("utf-8"))
        creds = Credentials.from_service_account_info(creds_json, scopes=["https://www.googleapis.com/auth/spreadsheets"])
        ws = gspread.authorize(creds).open_by_key(GSHEET_ID).worksheet("subscriptions")
        
        data = ws.get_all_values()
        idx = next((i for i, r in enumerate(data) if r[0] == uid), -1)
        now_ts = datetime.now(pytz.timezone("Asia/Almaty")).strftime("%d.%m.%Y %H:%M")

        if idx == -1:
            row = [
                uid, "active", "trial", (datetime.now() + timedelta(days=3)).strftime("%d.%m.%Y"),
                birth or "", now_ts, now_ts, user.username or "", user.first_name or "",
                user.last_name or "", datetime.now().strftime("%d.%m.%Y"), last_ym or "", "Asia/Almaty", ""
            ]
            ws.append_row(row)
            return row
        else:
            idx += 1
            ws.update_cell(idx, 7, now_ts)
            if birth: ws.update_cell(idx, 5, birth)
            if last_ym: ws.update_cell(idx, 12, last_ym)
            return ws.row_values(idx)
    except Exception as e:
        log.error(f"GSheet Error: {e}"); return None

# --- HANDLERS ---
async def start(u: Update, c: ContextTypes.DEFAULT_TYPE):
    sync_user(u)
    await u.message.reply_text("✨ Введите дату рождения (ДД.ММ.ГГГГ):",
        reply_markup=ReplyKeyboardMarkup([[KeyboardButton("📅 Мой прогноз")]], resize_keyboard=True))

async def handle_msg(u: Update, c: ContextTypes.DEFAULT_TYPE):
    text = u.message.text.strip()
    if len(text) == 10 and text.count(".") == 2:
        sync_user(u, birth=text)
        await u.message.reply_text(f"✅ Дата {text} сохранена! Нажмите 'Мой прогноз'.")
        return

    if text == "📅 Мой прогноз":
        user = sync_user(u)
        if not user or not user[4]:
            await u.message.reply_text("Сначала введите дату рождения!"); return
        
        bd = datetime.strptime(user[4], "%d.%m.%Y")
        now = datetime.now(pytz.timezone("Asia/Almaty"))
        lg = reduce9(bd.day + bd.month + now.year)
        lm = reduce9(lg + now.month)
        ld = reduce9(lm + now.day)
        od = reduce9(now.day + now.month + now.year)
        
        msg = f"📅 *Прогноз на {now.strftime('%d.%m.%Y')}*\n\n"
        if now.day in [10, 20, 30]:
            msg += "⚠️ *Внимание!* 10, 20, 30 числа — риск обнуления результатов. Не начинайте новое.\n\n"
        elif od in [3, 6]:
            msg += f"🌟 *Общий день {od}:* Благоприятный день для сделок и начинаний!\n\n"
        else:
            msg += f"🌐 *Общий день:* {od}\n\n"

        if user[11] != now.strftime("%m.%Y"):
            info = DESC_LG.get(str(lg), {})
            msg += f"✨ *Личный год {lg}: {info.get('n','')}*\n{info.get('d','')}\n\n*Рекомендации:* {info.get('r','')}\n*В минус:* {info.get('m','')}\n\n"
            msg += f"🌙 *Личный месяц {lm}:*\n{DESC_LM.get(str(lm), '')}\n\n"
            sync_user(u, last_ym=now.strftime("%m.%Y"))
        
        msg += f"📍 *Личный день {ld}:*\n{DESC_LD.get(str(ld), '')}"
        await u.message.reply_text(msg, parse_mode="Markdown")

# --- СЕРВЕР И ВЕБХУК ---
app = Flask(__name__)

# Инициализируем бота глобально
application = Application.builder().token(TELEGRAM_TOKEN).build()
application.add_handler(CommandHandler("start", start))
application.add_handler(MessageHandler(filters.TEXT & ~filters.COMMAND, handle_msg))

# Глобальная переменная для цикла событий
main_loop = asyncio.get_event_loop()

@app.route("/webhook", methods=["POST"])
def webhook():
    """Обработка входящих запросов от Telegram"""
    try:
        update_data = request.get_json(force=True)
        update = Update.de_json(update_data, application.bot)
        
        # Вместо создания нового цикла, пробрасываем задачу в уже работающий
        asyncio.run_coroutine_threadsafe(application.process_update(update), main_loop)
        
    except Exception as e:
        log.error(f"Ошибка в вебхуке: {e}")
        
    return "OK", 200

@app.route("/")
def index():
    return "Бот активен и работает!", 200

if __name__ == "__main__":
    # 1. Настройка вебхука перед запуском Flask
    async def setup_webhook():
        await application.initialize()
        await application.bot.set_webhook(f"{PUBLIC_URL}/webhook")
        log.info(f"Вебхук установлен на: {PUBLIC_URL}/webhook")

    # Запускаем инициализацию в основном цикле
    main_loop.run_until_complete(setup_webhook())

    # 2. Запуск Flask сервера
    # На Render переменная PORT устанавливается автоматически
    port = int(os.environ.get("PORT", 10000))
    app.run(host="0.0.0.0", port=port)