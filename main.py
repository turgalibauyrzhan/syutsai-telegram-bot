import os, json, base64, logging, asyncio
from datetime import datetime, timedelta
import pytz
from flask import Flask, request
from telegram import Update, ReplyKeyboardMarkup, KeyboardButton
from telegram.ext import Application, CommandHandler, MessageHandler, ContextTypes, filters
import gspread
from google.oauth2.service_account import Credentials

# --- НАСТРОЙКИ ---
logging.basicConfig(level=logging.INFO)
log = logging.getLogger(__name__)

TELEGRAM_TOKEN = os.getenv("TELEGRAM_TOKEN")
PUBLIC_URL = os.getenv("PUBLIC_URL", "").rstrip('/')
GSHEET_ID = os.getenv("GSHEET_ID")
GOOGLE_SA_JSON_B64 = os.getenv("GOOGLE_SA_JSON_B64")

# --- БАЗА ДАННЫХ (ПОЛНЫЕ ТЕКСТЫ ИЗ ВАШИХ ТАБЛИЦ) ---

DESC_LG = {
    "1": {"n": "Начало нового цикла", "d": "Время выбора направления на 9 лет. Приходит самый мощный энергопоток.", "r": "Открывай свое дело, развивай лидерство, бери ответственность.", "m": "Жжение в области сердца, депрессия, чувство пустоты."},
    "2": {"n": "Год построения отношений и дипломатии", "d": "Период перемен в отношениях. Старое уходит — не цепляйся.", "r": "Учись дипломатии, сохраняй гибкость, смотри на мир реалистично.", "m": "Депрессия и болезненные переживания разрывов."},
    "3": {"n": "Год анализа и успеха", "d": "Пробуждается аналитическое мышление. Время ведения учета.", "r": "Действуй через расчет, планируй шаги на год вперед.", "m": "Лень, азарт, корысть, стремление к быстрой выгоде."},
    "4": {"n": "Год мистических событий", "d": "Год постановки целей через неудовлетворенность.", "r": "Ставь цели, будь креативным, трудись честно.", "m": "Желание мошенничать, риск судебных дел, апатия."},
    "5": {"n": "Год удачи и масштабирования", "d": "Год правильных коммуникаций и фортуны.", "r": "Расширяй бизнес, путешествуй, строй связи через логику.", "m": "Борьба за справедливость, экстремизм, непостоянство."},
    "6": {"n": "Год успеха и комфорта", "d": "Время любви, инвестиций и пересмотра ценностей.", "r": "Дари любовь, инвестируй, создавай комфорт для других.", "m": "Мстительность, лень, неразборчивые связи, долги."},
    "7": {"n": "Год трансформации и кризиса", "d": "Отработка кармы. Время глубокого внутреннего роста.", "r": "Принимай ответственность, больше двигайся, не начинай новых дел.", "m": "Хаос, непонимание, отчаяние, рассеянность."},
    "8": {"n": "Год труда и обучения", "d": "Успех через дисциплину. Всё наработанное будет служить долго.", "r": "Учись, трудись, покупай недвижимость. Не бери кредиты.", "m": "Ограничения, усталость, перегрузка или бездействие."},
    "9": {"n": "Год служения и разрушения", "d": "Завершение цикла. Подведение итогов.", "r": "Прости обиды, отдай старое, помогай людям, следи за здоровьем.", "m": "Эмоциональные всплески, разрушение без созидания."}
}

DESC_LM = {
    "1": "Хороший месяц для начала дел. Важна стратегия, планирование и лидерство.",
    "2": "Месяц дипломатии. Активизируется энергия воспоминаний. Пей больше воды.",
    "3": "Месяц анализа и успеха. Сначала думай, потом делай. Хорошо для обучения.",
    "4": "Месяц мистических событий. Ставь цели, будь креативен и честен.",
    "5": "Месяц масштабирования. Время для бизнеса, поездок и новых связей.",
    "6": "Месяц любви и успеха. Интуиция на деньги. Удачно для брака и инвестиций.",
    "7": "Месяц кризиса или трансформации. Жизнь в дисциплине. Полезны йога и практики.",
    "8": "Месяц труда и обучения. Контролируй финансы, повышай квалификацию.",
    "9": "Месяц благодарности. Время подводить итоги и отпускать лишнее."
}

DESC_LD = {
    "1": "День новых начинаний. Реализуй стратегию, будь смелым.",
    "2": "День понимания и дипломатии. Слушай искренне, налаживай связи.",
    "3": "День анализа и планирования. Благоприятно для врачей и процедур.",
    "4": "День мистических событий. Будь честен, ставь цели.",
    "5": "День масштабирования. Отлично для торговли и переговоров.",
    "6": "День творчества и любви. Создавай комфорт, дари заботу.",
    "7": "День трансформации. Дисциплина тела: ходьба, йога.",
    "8": "День обучения и труда. Получай навыки, не бери кредиты.",
    "9": "День здоровья и благодарности. Баня, массаж, отдача долгов."
}

# --- ЛОГИКА ---
def reduce9(n):
    while n > 9: n = sum(map(int, str(n)))
    return n

def get_full_calc(bd_str):
    tz = pytz.timezone("Asia/Almaty")
    now = datetime.now(tz)
    bd = datetime.strptime(bd_str, "%d.%m.%Y")
    lg = reduce9(bd.day + bd.month + now.year)
    lm = reduce9(lg + now.month)
    ld = reduce9(lm + now.day)
    od = reduce9(now.day + now.month + now.year)
    return {"lg": str(lg), "lm": str(lm), "ld": str(ld), "od": od, 
            "ym": now.strftime("%m.%Y"), "today": now.strftime("%d.%m.%Y"), "day_num": now.day}

def sync_user(update: Update, birth=None, last_ym=None):
    try:
        user = update.effective_user
        uid = str(user.id)
        creds_json = json.loads(base64.b64decode(GOOGLE_SA_JSON_B64).decode("utf-8"))
        creds = Credentials.from_service_account_info(creds_json, scopes=["https://www.googleapis.com/auth/spreadsheets"])
        ws = gspread.authorize(creds).open_by_key(GSHEET_ID).worksheet("subscriptions")
        
        data = ws.get_all_values()
        idx = next((i for i, r in enumerate(data) if r[0] == uid), -1)
        now_ts = datetime.now(pytz.timezone("Asia/Almaty")).strftime("%d.%m.%Y %H:%M")

        if idx == -1:
            row = [uid, "active", "trial", (datetime.now()+timedelta(days=3)).strftime("%d.%m.%Y"), 
                   birth or "", now_ts, now_ts, user.username or "", user.first_name or "", 
                   user.last_name or "", datetime.now().strftime("%d.%m.%Y"), last_ym or "", "Asia/Almaty", ""]
            ws.append_row(row)
            return row
        else:
            idx += 1
            ws.update_cell(idx, 7, now_ts) # last_seen_at
            if birth: ws.update_cell(idx, 5, birth)
            if last_ym: ws.update_cell(idx, 12, last_ym)
            return ws.row_values(idx)
    except Exception as e:
        log.error(f"GSheet Error: {e}"); return None

# --- HANDLERS ---
async def start(u: Update, c: ContextTypes.DEFAULT_TYPE):
    sync_user(u)
    await u.message.reply_text("✨ Добро пожаловать! Введите вашу дату рождения (ДД.ММ.ГГГГ):",
        reply_markup=ReplyKeyboardMarkup([[KeyboardButton("📅 Мой прогноз")]], resize_keyboard=True))

async def handle_msg(u: Update, c: ContextTypes.DEFAULT_TYPE):
    text = u.message.text.strip()
    if len(text) == 10 and text.count(".") == 2:
        sync_user(u, birth=text)
        await u.message.reply_text(f"✅ Дата {text} сохранена! Нажмите 'Мой прогноз'.")
        return

    if text == "📅 Мой прогноз":
        user_data = sync_user(u)
        if not user_data or not user_data[4]:
            await u.message.reply_text("Пожалуйста, введите дату рождения."); return
        
        res = get_full_calc(user_data[4])
        is_first_time = (user_data[11] != res["ym"])
        
        msg = f"📅 *Прогноз на {res['today']}*\n\n"
        
        # Общий день (ОД)
        if res['day_num'] in [10, 20, 30]:
            msg += "⚠️ *Неблагоприятная дата:* Сегодня 10, 20 или 30 число. Риск обнуления результатов.\n\n"
        elif res['od'] in [3, 6]:
            msg += f"🌟 *Общий день {res['od']}:* Очень благоприятный день для сделок и начинаний!\n\n"
        else:
            msg += f"🌐 *Общий день:* {res['od']}\n\n"

        # Полный прогноз (ЛГ и ЛМ) - раз в месяц
        if is_first_time:
            lg_info = DESC_LG.get(res['lg'], {})
            msg += f"✨ *Личный год {res['lg']}: {lg_info.get('n','')}*\n"
            msg += f"{lg_info.get('d','')}\n\n"
            msg += f"*Рекомендации:* {lg_info.get('r','')}\n"
            msg += f"*В минусе:* {lg_info.get('m','')}\n\n"
            msg += f"{DESC_LM.get(res['lm'], '')}\n\n"
            sync_user(u, last_ym=res["ym"])
        
        # Личный день (ЛД) - всегда
        msg += f"{DESC_LD.get(res['ld'], 'Описание готовится...')}"
        
        await u.message.reply_text(msg, parse_mode="Markdown")

# --- WEBHOOK LOGIC ---
app = Flask(__name__)
application = Application.builder().token(TELEGRAM_TOKEN).build()
application.add_handler(CommandHandler("start", start))
application.add_handler(MessageHandler(filters.TEXT & ~filters.COMMAND, handle_msg))

@app.route("/webhook", methods=["POST"])
def webhook():
    # Используем run_coroutine_threadsafe для стабильности цикла в Flask
    update = Update.de_json(request.get_json(force=True), application.bot)
    asyncio.run_coroutine_threadsafe(application.process_update(update), main_loop)
    return "OK", 200

if __name__ == "__main__":
    main_loop = asyncio.new_event_loop()
    asyncio.set_event_loop(main_loop)
    
    # Инициализация вебхука
    main_loop.run_until_complete(application.initialize())
    main_loop.run_until_complete(application.bot.set_webhook(f"{PUBLIC_URL}/webhook"))
    
    # Запуск Flask
    app.run(host="0.0.0.0", port=int(os.environ.get("PORT", 10000)))